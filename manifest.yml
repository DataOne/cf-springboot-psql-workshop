---
applications:
  - name: spring-psql-api
    memory: 1G
    path: target/demo-0.0.1-SNAPSHOT.jar
    services:
      - psql-azure

    env:
      JBP_CONFIG_SPRING_AUTO_RECONFIGURATION: '{enabled: false}'
      JBP_CONFIG_OPEN_JDK_JRE: '{jre: { version: 11.+ }}'
      SPRING_PROFILES_ACTIVE: cloud